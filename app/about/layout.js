export const metadata = {
    title: 'About Us',
    description: 'Learn about the Mathematics Society team and our mission.'
};

export default function AboutLayout({ children }) {
    return <>{children}</>;
}