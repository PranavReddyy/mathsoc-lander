export const metadata = {
    title: 'Community Service',
    description: 'Discover how MathSoc gives back through our community service initiatives, including tutoring programs, math workshops for local schools, and educational outreach activities.'
};

export default function CommunityServiceLayout({ children }) {
    return <>{children}</>;
}